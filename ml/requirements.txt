pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pytest>=7.0
openpyxl>=3.1

# Only needed for ml/training/extract_karsl_skeletons.py (real video -> landmark
# extraction). Not required for anything else in this project.
opencv-python>=4.8
mediapipe>=0.10